# AGENTS.md · 无限规则通用计算器

本文件记录项目协作原则与约束，供所有参与修改代码的人（包括 AI）遵守。

---

## 单测原则（必须遵守）

1. **单测用于规范代码行为**  
   单元测试是代码行为的契约。修改源码时，应使代码满足现有单测，而不是反过来改单测去迎合新代码。

2. **禁止随意修改单测**  
   在修改源码的过程中，**不允许**自行修改、删除、放宽或调整已有单测用例。  
   若认为某个单测需要变更（包括但不限于：修改用例参数/预期、删除用例、改断言逻辑），**必须先征求项目负责人（用户）的意见**，得到明确同意后再改。

3. **新增单测的要求**  
   撰写单测时，必须补充清晰注释，至少说明：
   - **场景**：在什么情况下触发
   - **参数 / 前置状态**：输入或初始状态是什么
   - **预期结果**：应该得到什么输出或状态
   - 如有特殊规则或边界，一并注明

4. **单测与功能模块对应**  
   每个核心模块（如 `hp`）应有独立的 `*_test.go`，覆盖主要路径与用户明确提出的案例。

---

## 模块化原则

- 血量计算是通用计算器的一个模块（`hp`），未来可继续增加其他规则模块（攻击、检定、先攻等）。
- 核心逻辑放在对应 package 中，尽量不依赖 Web/UI。
- Web 层只负责展示与 API，不包含业务计算。

---

## 修改后流程（必须遵守）

在修改了任意模块的代码后，必须按以下顺序执行：

1. **运行对应模块的单测**  
   例如修改了 `hp` 模块，则执行：`go test ./hp/ -count=1`  
   若修改涉及多个模块或公共入口，应运行相关包的测试（或 `go test ./...`）。

2. **单测全部通过后，自动编译最新产物**  
   产物需同时生成以下两个平台版本，并输出到项目 `产物/` 目录：
   - **macOS**：`GOOS=darwin GOARCH=amd64`（以及如有需要可再编 arm64）
   - **Windows**：`GOOS=windows GOARCH=amd64`（生成 `.exe`）

3. **单测失败时**  
   不得发布/覆盖产物；应先修复代码使单测通过，再编译。

推荐编译命令示例（在 `源码/无限规则计算器` 下）：

```bash
# 1. 单测（按修改的模块运行，或全量）
go test ./hp/ ./rest/ ./defense/ -count=1

# 2. 产物（通过后执行）
GOOS=darwin  GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-s -w" -o ../../../产物/无限规则计算器_macOS .
GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-s -w" -o ../../../产物/无限规则计算器.exe .
```

---

## 目录结构（当前）

```
无限规则计算器/
├── AGENTS.md          # 本文件
├── go.mod
├── main.go            # 入口
├── hp/                # 血量模块
│   ├── state.go
│   └── state_test.go
├── rest/              # 短休 / 长休模块（可扩展至意志力等）
│   ├── rest.go
│   └── rest_test.go
├── defense/           # 防御预设模块
│   ├── preset.go
│   └── preset_test.go
└── web/               # Web 界面与 API
    ├── handlers.go
    ├── page.go          # 血量
    └── defense_page.go  # 防御预设
```
